import React from "react";
import ReactDOM from "react-dom/client"; // ¡Ojo! esto cambia
import Button from "./Button";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<Button />);
